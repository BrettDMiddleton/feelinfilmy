function handleTag(_, target){
  document.getElementById("movie_tag_movie_id").value = target.id
}

function handleTagClick(_, target){
  document.getElementById("movie_tag_tag_id").value = target.id
}
