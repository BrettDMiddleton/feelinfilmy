
<div class="container">
  <div class="row">
    <% @tags.each do |tag|%>
      <div class="col-xs-12 col-sm-4">
        <a href="" class="btn-filmy"> <%= tag.name %></a>
      </div>
    <% end %>
  </div>
</div>

<h1>Movies#index</h1>
<p>Find me in app/views/movies/index.html.erb</p>

<ul>
<% @movies.each do |movie| %>
<li><%= link_to movie.title, movies_show_path(movie.id) %></li>
<%end %>

</ul>

